package org.virtuoso.escape.gui;

/** Used to track the current controller of the GUI and the current screen being displayed. */
public enum CurrentPage {
    NONE,
    LOGIN,
    INTRO,
    GAMEVIEW,
    CREDITS
}
